cmake_minimum_required(VERSION 2.8 FATAL_ERROR)

set(PROJECT_NAME ecs)
project(${PROJECT_NAME})

#set(HEADERS include/ecs.h)
#set(SOURCES )

#add_library(${PROJECT_NAME} STATIC ${HEADERS} ${SOURCES})
